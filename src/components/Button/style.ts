import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
	container: {
		padding: 16,
		alignItems: 'center',
		justifyContent: 'center',
		width: '100%',
		borderRadius: 8,
		height: 60,
		marginVertical: 10,
	},
	content: {
		flexDirection: 'row',
	},
	disabled: {
		opacity: 0.6,
	},
})
